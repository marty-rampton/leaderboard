services:
  postgres:
    image: postgres:16
    restart: unless-stopped
    env_file: .env.docker
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data
      - ./db/init:/docker-entrypoint-initdb.d

  backend:
    build: .
    env_file: .env.docker
    restart: unless-stopped
    depends_on:
      - postgres
    ports:
      - "3001:3001"

volumes:
  postgres-data:
